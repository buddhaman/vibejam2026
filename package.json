{
  "name": "agi-of-mythology",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client:wait\"",
    "dev:server": "NODE_ENV=development tsx watch --tsconfig tsconfig.server.json server/src/index.ts",
    "dev:client:wait": "node scripts/compress-gltf.mjs && node scripts/wait-for-colyseus.mjs && vite client",
    "dev:client": "vite client",
    "build": "npm run build:server && npm run build:client",
    "build:server": "tsc -p tsconfig.server.json && node -e \"require('fs').writeFileSync('dist/server/package.json', JSON.stringify({ type: 'commonjs' }))\"",
    "build:client": "node scripts/compress-gltf.mjs && vite build client",
    "analyze:glb": "node scripts/analyze-glb-assets.mjs",
    "models:compress": "node scripts/compress-gltf.mjs",
    "models:compress:watch": "node scripts/compress-gltf.mjs --watch",
    "start": "node dist/server/src/index.js"
  },
  "dependencies": {
    "@colyseus/schema": "^4.0.19",
    "@colyseus/sdk": "^0.17.36",
    "@colyseus/ws-transport": "^0.17.11",
    "colyseus": "^0.17.8",
    "express": "^4.21.2",
    "three": "^0.174.0"
  },
  "devDependencies": {
    "@gltf-transform/cli": "^4.3.0",
    "@types/express": "^4.17.21",
    "@types/node": "^22.13.10",
    "@types/three": "^0.174.0",
    "chokidar": "^4.0.3",
    "concurrently": "^9.1.2",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vite": "^6.2.2"
  }
}
